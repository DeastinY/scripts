# Get current information with xmodmap -pke > ~/.Xmodmap and add the following: Get Keycode via xev
# If the key was used for something else before check its groups with xmodmap, then add e.g. clear control and readd the keys you still want there: add control = Control_L
keycode 66 = Mode_switch
keysym a = a A Left
keysym e = e E Right
keysym comma = comma semicolon Up
keysym o = o O Down
